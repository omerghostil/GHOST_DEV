rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // כל הגישה לנתונים מתבצעת דרך Cloud Functions בלבד (server-side)
    // הקליינט לא ניגש ישירות ל-Firestore
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
